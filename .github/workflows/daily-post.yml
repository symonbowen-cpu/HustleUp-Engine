name: Daily Instagram Post

on:
  schedule:
    # 9am Pacific Time (UTC-7/8) = 4pm or 5pm UTC
    # Adjust for your timezone
    - cron: '0 16 * * *'
  workflow_dispatch:  # manual trigger for testing
    inputs:
      format:
        description: 'Post format'
        type: choice
        options:
          - auto
          - reel
          - static
        default: auto
  issue_comment:
    types: [created]  # needed for /approve and /skip commands

jobs:
  post:
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    permissions:
      contents: write
      issues: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install ffmpeg (reel rendering)
        run: sudo apt-get update -qq && sudo apt-get install -y -qq ffmpeg

      - name: Generate post
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          # manual runs can force a format; 'auto' / scheduled runs use themes.json
          FORMAT: ${{ inputs.format != 'auto' && inputs.format || '' }}
        run: npm run generate

      - name: Commit generated image
        run: |
          git config user.name "HustleUp Bot"
          git config user.email "bot@hustleup.us"
          git add -A out/pending/ content/state.json
          git diff --staged --quiet || git commit -m "chore: daily post $(date -u +%Y-%m-%d)"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Get commit SHA
        id: sha
        run: echo "sha=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT

      - name: Publish or create approval issue
        if: vars.AUTO_PUBLISH == 'true'
        env:
          IG_USER_ID: ${{ secrets.IG_USER_ID }}
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
          IG_GRAPH_HOST: ${{ vars.IG_GRAPH_HOST || 'graph.facebook.com' }}
          GITHUB_SHA: ${{ steps.sha.outputs.sha }}
        run: npm run publish:ig

      - name: Create approval issue
        if: vars.AUTO_PUBLISH != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const post = JSON.parse(fs.readFileSync('out/pending/post.json', 'utf8'));
            const sha = '${{ steps.sha.outputs.sha }}';
            const raw = `https://raw.githubusercontent.com/${context.repo.owner}/${context.repo.repo}/${sha}/out/pending`;

            const isReel = post.format === 'reel';
            const preview = isReel
              ? `![Reel Preview](${raw}/preview.gif)\n\n🎬 **[▶ Watch the full reel with sound](${raw}/post.mp4)** (${post.media.durationSec}s, track: ${post.media.music})`
              : `![Post Preview](${raw}/post.png)`;

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `${isReel ? '🎬 Daily Reel' : '📸 Daily Post'} — ${new Date().toLocaleDateString('en-US', {weekday:'long', month:'long', day:'numeric'})}`,
              body: `## Today's HustleUp ${isReel ? 'Reel' : 'Post'}\n\n**Pillar:** ${post.pillar}\n**Topic:** ${post.topic}\n\n${preview}\n\n**Caption:**\n\`\`\`\n${post.caption}\n\`\`\`\n\n---\n\nComment \`/approve\` to publish to Instagram.\nComment \`/skip\` to discard this post.`,
              labels: ['content-approval']
            });

  approve:
    runs-on: ubuntu-latest
    if: github.event_name == 'issue_comment' && contains(github.event.comment.body, '/approve') && contains(github.event.issue.labels.*.name, 'content-approval')
    permissions:
      contents: read
      issues: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Get image SHA from issue
        id: get_sha
        uses: actions/github-script@v7
        with:
          result-encoding: string
          script: |
            const body = context.payload.issue.body;
            const match = body.match(/\/([a-f0-9]{40})\/out/);
            return match ? match[1] : 'main';

      - name: Publish to Instagram + Facebook
        env:
          IG_USER_ID: ${{ secrets.IG_USER_ID }}
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
          IG_GRAPH_HOST: ${{ vars.IG_GRAPH_HOST || 'graph.facebook.com' }}
          GITHUB_SHA: ${{ steps.get_sha.outputs.result }}
        run: npm run publish:ig

      - name: Close issue
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.issue.number,
              body: '✅ Published to Instagram + Facebook!'
            });
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.issue.number,
              state: 'closed'
            });

  skip:
    runs-on: ubuntu-latest
    if: github.event_name == 'issue_comment' && contains(github.event.comment.body, '/skip') && contains(github.event.issue.labels.*.name, 'content-approval')
    permissions:
      issues: write

    steps:
      - name: Close issue
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.issue.number,
              body: '⏭️ Post skipped.'
            });
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.issue.number,
              state: 'closed'
            });
